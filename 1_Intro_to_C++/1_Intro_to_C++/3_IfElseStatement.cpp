#include <iostream>
#include <string>
/*
using namespace std;

int main()
{
	// Write an If statement that will check for voters voting age
	// Declare the input variable
	int age;
	// Print the question
	cout << "How old are you sir? \n";
	// User input prompt and store value in the decleared variable
	cin >> age;
	// Print the voter's age
	cout << "So... You are " << age << " years old \n";
	if(age<18)
	{
		cout << "Sorry, you cannot vote \n";
	}
	else if (age == 18)
	{
		cout << "Sorry, close but not old enough! \n Try again next year \n";
	}
	else
	{
		cout << "Thank you sir, go right ahead and cast your vote! \n";
	}

	int score;
	cout << "Please enter your score:" << endl;
	cin >> score;

	if (score < 60 && score >= 0)
	{
		cout << "F" << endl;
	}
	else if (score >= 60 && score < 70)
	{
		cout << "D" << endl;
	}
	else if (score >= 70 && score < 80)
	{
		cout << "C" << endl;
	}
	else if (score >= 80 && score < 90)
	{
		cout << "B" << endl;
	}
	else if (score >= 90 && score <= 100)
	{
		cout << "A" << endl;
	}
	else
	{
		cout << "Please enter a number that is within 0 to 100.\n THANK YOU!" << endl;
	}
	system("pause");
}
*/